<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. The real colors come from the Compose theme;
         this just provides a dark, edge-to-edge friendly system shell. -->
    <style name="Theme.Claudeophobia" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/claude_background</item>
    </style>
</resources>
